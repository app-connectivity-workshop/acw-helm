---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ include "kiali-operator.name" . }}-operator
  namespace: {{ include "tempo-operator.namespace" . }}
  annotations:
    {{- include "kiali-operator.argocd-syncwave" . | nindent 4 }}
spec:
  channel: {{ .Values.kiali.subscription.channel }}
  installPlanApproval: {{ .Values.kiali.subscription.installPlanApproval }}
  name: {{ .Values.kiali.subscription.name }}
  source: {{ .Values.kiali.subscription.source }}
  sourceNamespace: {{ .Values.kiali.subscription.sourceNamespace }}
