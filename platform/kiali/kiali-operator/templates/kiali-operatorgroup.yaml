---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: "{{ include "kiali-operator.name" . }}-operatorgroup"
  namespace: {{ include "kiali-operator.namespace" . }}
  annotations:
    {{- include "kiali-operatorgroup.argocd-syncwave" . | nindent 4 }}
spec:
  targetNamespaces:
    - {{ include "kiali-operator.namespace" . }}
