---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: "{{ include "tempo-operator.name" . }}-operatorgroup"
  namespace: {{ include "tempo-operator.namespace" . }}
  annotations:
    {{- include "tempo-operatorgroup.argocd-syncwave" . | nindent 4 }}
spec:
  targetNamespaces:
    - {{ include "tempo-operator.namespace" . }}
